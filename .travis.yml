language: node_js
node_js:
  - 10

cache:
  directories:
    - node_modules

script:
  - npm test
  - npm run build

before_deploy:
  - sh publish.sh

deploy:
  provider: pages
  local_dir: docs
  skip_cleanup: true
  github_toke: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master